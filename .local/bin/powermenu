#! /bin/sh

chosen=$(printf "Suspend\nReboot\nShutdown\nLog Out" | tofi)

case "$chosen" in
	"Suspend") systemctl suspend-then-hibernate ;;
	"Reboot") reboot ;;
	"Shutdown") poweroff ;;
	"Log Out") hyprctl dispatch exit ;;
	*) exit 1 ;;
esac
